use super::Problem;

fn champernowne_digit(n: usize) -> u8 {
    let mut total_digits = 1;

    return total_digits;
}

pub struct ChampernownesConstantProblem {}

impl Problem for ChampernownesConstantProblem {
    fn solve(&self) -> String {
        return format!("");
    }
}