use super::Problem;

// fn add_chain()

// fn find_chain()

pub struct EfficientExponentiationProblem {}

impl Problem for EfficientExponentiationProblem {
    fn solve(&self) -> String {
        let mut chains: Vec<Vec<u32>> = vec![vec![1]];
        let (mut index, mut position) = (0usize, 0usize);

        format!("")
    }
}
